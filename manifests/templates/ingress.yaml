---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springyyyyboot                  # Ingress name
  labels:
    app: springyyyyboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springyyyyboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springyyyyboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
